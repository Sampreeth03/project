openapi: 3.0.3
info:
  title: RELABTeams Member 1 User APIs
  version: 1.0.0
  description: Aggregated OpenAPI file for Member 1 user-related endpoints (excluding login, registration, and onboarding).
servers:
  - url: /
    description: Relative to deployment origin
paths:
  $ref: ./paths/member1-user-paths.yaml#/paths
components:
  $ref: ./paths/member1-user-schemas.yaml#/components
