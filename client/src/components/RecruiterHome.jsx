import React from 'react';

const RecruiterHome = () => {
    return (
        <div style={{ padding: '40px', textAlign: 'center', color: 'white', backgroundColor: 'black', minHeight: '100vh' }}>
            <h1>Recruiter Dashboard (Placeholder)</h1>
            <p>You have Recruiter privileges. (Conversion pending)</p>
            <p>This page will fetch data from /api/recruiter-dashboard.</p>
        </div>
    );
};

export default RecruiterHome;
// CRITICAL: This line ensures the file provides the necessary default export.